import {useEffect, useState} from 'react';
import { getDownloadURL, getStorage, ref } from "firebase/storage";

export default function getImage(){
    const [x, y] = useState('nie działa')
    return (x)
}